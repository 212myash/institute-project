# MongoDB Atlas URI (example only)
# Never commit real credentials.
MONGO_URI=mongodb+srv://<db_user>:<db_password>@cluster0.example.mongodb.net/institute

# JWT secret (example only)
JWT_SECRET=replace_with_a_long_random_secret

# App runtime
PORT=5000
NODE_ENV=development
